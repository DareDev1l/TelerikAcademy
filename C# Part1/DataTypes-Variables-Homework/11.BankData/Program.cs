using System;

// A bank account has a holder name (first name, middle name and last name), available amount of money (balance),
// bank name, IBAN, 3 credit card numbers associated with the account.
// Declare the variables needed to keep the information for a single bank account using the appropriate data types
// and descriptive names.

class Program
{
    static void Main(string[] args)
    {
        string ownerFirstName;
        string ownerMiddleName;
        string ownerLastName;
        
        // money should be kept in the most precision type , which is decimal
        decimal balance;
        string bankName;
        string IBAN;

        // There won't be any calculations of the card numbers . Otherwise we can use long
        string cardNumberOne;
        string cardNumberTwo;
        string cardNumberThree;
    }
}
